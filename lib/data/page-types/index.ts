/**
 * ============================================
 * 页面类型数据查询统一导出
 * ============================================
 *
 * 模块组织：
 * - info.ts - 页面类型信息查询（包含完整缓存逻辑）
 * - games.ts - 页面类型的游戏列表查询（包含完整缓存逻辑）
 *
 * 注意：此模块不使用单独的 cache.ts，每个函数直接实现缓存
 * 避免多层缓存嵌套，提高性能
 */

// 页面类型信息
export { getAllPageTypes, getPageTypeInfo } from "./info"

// 页面类型游戏列表
export { getPageTypeGames } from "./games"
